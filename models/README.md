This folder should contain exported model artifacts used by the service.

- `model.pkl` : Primary trained model serialized with `joblib` or `pickle`.

Place your trained model file here (for example by running a training script or
by copying the output of your notebook). The service expects `models/model.pkl`.
